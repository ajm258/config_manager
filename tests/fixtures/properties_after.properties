# Formatting changes are intentionally ignored.

server.port = 8080
spring.profiles.active = dashboard
application.name=test-app

service.url = http://localhost:8080/api

empty.value =

unchanged.value = same
changed.value = after

new.value=vendor-default
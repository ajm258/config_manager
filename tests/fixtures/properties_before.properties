# Stage 2 parser/comparison fixture

server.port=8080
spring.profiles.active=no-dashboard
application.name = test-app

# URL contains ':' but ':' is not the delimiter
service.url=http://localhost:8080/api

empty.value=

unchanged.value=same
changed.value=before
removed.value=keep-me